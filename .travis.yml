language: c

dist: trusty

cache: ccache

compiler:
  - gcc

addons:
  apt:
    packages:
      - valgrind
      - lcov
      - -t backports swig3.0

script:
  - make
  - make test
  - make NightlyCoverage

after_success:
  - bash <(curl -s https://codecov.io/bash)
